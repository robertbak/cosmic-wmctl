[package]
name = "cosmic-wmctl"
version = "0.2.0"
edition = "2024"
rust-version = "1.86"
license = "MIT"
description = "Small CLI for managing COSMIC windows and workspaces"
publish = false

[workspace]
members = [
    "cosmic-wmctl-config",
]
# Vendored third-party code is a path dependency, not a workspace member:
# keeping it out of `members` means `cargo clippy --workspace` does not lint
# it, so lint churn from newer toolchains cannot break our CI.
exclude = [
    "vendor/cosmic-protocols",
    "vendor/cosmic-protocols/client-toolkit",
]

[dependencies]
anyhow = "1.0.104"
clap = { version = "4.6", features = ["derive"] }
cosmic-client-toolkit = { path = "vendor/cosmic-protocols/client-toolkit" }
globset = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sctk = { package = "smithay-client-toolkit", version = "0.21.1" }
toml = "1"
wayland-client = "0.31.15"
wayland-protocols = { version = "0.32.13", features = ["client", "staging"] }

[lints.clippy]
dbg_macro = "deny"
todo = "deny"
unwrap_used = "deny"

[lints.rust]
unsafe_code = "forbid"

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
panic = "abort"
